import { PersonalInformations } from './personal-informations';

describe('PersonalInformations', () => {
  it('should create an instance', () => {
    expect(new PersonalInformations()).toBeTruthy();
  });
});
