import { ConyugeInformation } from './conyuge-information';

describe('ConyugeInformation', () => {
  it('should create an instance', () => {
    expect(new ConyugeInformation()).toBeTruthy();
  });
});
