import { CurrencyOperations } from './currency-operations';

describe('CurrencyOperations', () => {
  it('should create an instance', () => {
    expect(new CurrencyOperations()).toBeTruthy();
  });
});
