import { LocationInformation } from './location-information';

describe('LocationInformation', () => {
  it('should create an instance', () => {
    expect(new LocationInformation()).toBeTruthy();
  });
});
