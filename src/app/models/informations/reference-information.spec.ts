import { ReferenceInformation } from './reference-information';

describe('ReferenceInformation', () => {
  it('should create an instance', () => {
    expect(new ReferenceInformation()).toBeTruthy();
  });
});
