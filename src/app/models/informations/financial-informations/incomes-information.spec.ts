import { IncomesInformation } from './incomes-information';

describe('IncomesInformation', () => {
  it('should create an instance', () => {
    expect(new IncomesInformation()).toBeTruthy();
  });
});
