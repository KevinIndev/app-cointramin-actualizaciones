import { PassivesInformation } from './passives-information';

describe('PassivesInformation', () => {
  it('should create an instance', () => {
    expect(new PassivesInformation()).toBeTruthy();
  });
});
