import { ExpensesInformation } from './expenses-information';

describe('ExpensesInformation', () => {
  it('should create an instance', () => {
    expect(new ExpensesInformation()).toBeTruthy();
  });
});
